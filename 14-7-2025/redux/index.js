// redux is js library. it is predficatable state container.
// it is state management library.

// vuex rjx

// why redux

// react has a local state (via useState) --> works fine only with small apps.

// in larger, passing props across many componanent (prop drilling) becomes complex

// keeps global state in single store and allow component to access and updatestate from anywhere

// core concepts of redux
//  store --> it is single source of truth
//  state --> actual data (an object) held inside the store.
//  action --> plain js object that describes what need to happen.
// reducer --> pure function that takes previous state and an action and returns the next daa

// dispatch --> send actions
// selector
// middleware

